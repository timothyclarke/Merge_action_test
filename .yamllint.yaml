---
extends: default

rules:
  line-length: disable
  document-start: disable
  comments: disable
  comments-indentation: disable
  empty-lines: {max: 1}
  truthy:
    allowed-values: ["true", "false", "on"]
